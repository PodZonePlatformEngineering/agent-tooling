# Working repo clones + materialised session context — never committed
.workspace/

# macOS
.DS_Store

# Python
__pycache__/
*.pyc
*.pyo

# Local tool state
# logs/ is WORKING STATE, never committed (PROJ-039/T-093, operator decision
# 2026-07-09 — reverses T-062/T-068's narrower `logs/*.log` accommodation).
# Durability for completed sessions' logs is the agent-telemetry repo push
# (lib/telemetry_repo), not a tracked copy here. Trainee repos are the one
# exception: they commit their live working tree wholesale to a session
# branch (lib/session_guard.py stage_session_logs still force-stages their
# sid-keyed logs before that wholesale add) — unaffected by this ignore,
# since a force-add always overrides it.
logs/
.env

# Per-home-repo scoped env block (plaintext secrets until PROJ-003) — NEVER commit.
# Lives only on the workstation; carries PODZONE_QDRANT_APIKEY / SECRETCTL_PASSWORD.
# The committed .claude/settings.json carries hooks only; primitives/ + lib/ are
# resident under .claude/ (self-contained, no AGENT_TOOLING_DIR — PROJ-039 C2-v2.1).
.claude/settings.local.json
